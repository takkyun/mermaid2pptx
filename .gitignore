# Built binary (produced by `go build -o mermaid2pptx ./cmd/mermaid2pptx`)
/mermaid2pptx
/mermaid2pptx.exe

# Release build output (produced locally by the release build steps)
/dist/

# Internal / non-public material (kept out of version control on purpose)
/_sandbox/

# OS / editor cruft
.DS_Store
